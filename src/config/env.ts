// import { z } from "zod";

// const envSchema = z.object({
//   JWT_ACCESS_SECRET: z.string().min(1, "JWT_ACCESS_SECRET is required in .env"),
//   JWT_ACCESS_EXPIRES_IN: z.string().default("15m"),
// });

// export const env = envSchema.parse(process.env);
