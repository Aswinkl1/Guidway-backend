export const DOMAIN_ERRORS_MESSAGE = {
  USER: {
    ADMIN_BLOCK_RESTRICTED: "Admin cannot be blocked",
    ALREADY_BLOCKED: "User already blocked",
    NOT_BLOCKED: "User is not blocked",
  },
} as const;
